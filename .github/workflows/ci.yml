name: ci

on:
  push:
    branches:
      - ntmc-staging
jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [lts/*]

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@main

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.primeverse.id
          username: ntmc-production@ntmc.scanpedia.id
          password: ntmc-production
          port: 21


#      - name: Run tests 🧪
#        run: npm run test
