name: ci

on:
  push:
    branches:
      - production
  pull_request:
    branches:
      - production

jobs:
  ci:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest]
        node: [lts/*]

    steps:
      - name: Checkout 🛎
        uses: actions/checkout@main

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.primeverse.id
          username: staging-ntmc@staging-ntmc.scanpedia.id
          password: mamangsekayu97
          port: 21


#      - name: Run tests 🧪
#        run: npm run test
